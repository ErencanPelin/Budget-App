<Window x:Class="BudgetApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BudgetApp.MVVM.View"
        mc:Ignorable="d"
        Title="Pillow Budget" Height="450" Width="800"
        ResizeMode="NoResize">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="80"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid HorizontalAlignment="Stretch" Margin="15, 10">
            <TextBlock Text="Pillow Budget" Style="{StaticResource TitleText}"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Bottom">
                <RadioButton Content="Home" VerticalAlignment="Center" Style="{StaticResource NavigationButton }"/>
                <RadioButton Content="Budget" VerticalAlignment="Center" Style="{StaticResource NavigationButton}"/>
            </StackPanel>
        </Grid>
        <Border Grid.Row="1" Background="Black">
            
        </Border>
    </Grid>
</Window>
